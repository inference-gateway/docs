'use client';

import RegistryMarkdown from '@/markdown/registry.mdx';

export default function RegistryContent() {
  return (
    <div className="prose max-w-none">
      <RegistryMarkdown />
    </div>
  );
}
