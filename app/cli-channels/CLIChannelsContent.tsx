'use client';

import CLIChannelsMarkdown from '@/markdown/cli-channels.mdx';

export default function CLIChannelsContent() {
  return (
    <div className="prose max-w-none">
      <CLIChannelsMarkdown />
    </div>
  );
}
